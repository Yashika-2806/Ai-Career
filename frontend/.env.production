# Backend API URL - Using Vercel proxy to avoid HTTPS/HTTP mixed content
VITE_API_URL=/api

# Vercel serverless function at /api proxies to backend
# This is the ONLY way to avoid browser blocking HTTP from HTTPS
