╔════════════════════════════════════════════════════════════════════╗
║          🚨 URGENT FIX - RUN THESE COMMANDS NOW                      ║
╚════════════════════════════════════════════════════════════════════╝

PROBLEM: Nginx is STILL using PORT 5000 instead of 5001!
         Also missing SSL certificate ("connection is not private")

═══════════════════════════════════════════════════════════════════════

🔥 STEP 1: FIX NGINX PORT (Run on server RIGHT NOW)
───────────────────────────────────────────────────────────────────────

ssh root@ai.gladsw.cloud "cd /var/www/Ai-Career && git pull && bash emergency-fix-nginx.sh"

This will:
✅ Force Nginx to use PORT 5001
✅ Fix 502 Bad Gateway errors
✅ Reload Nginx properly


═══════════════════════════════════════════════════════════════════════

🔒 STEP 2: FIX SSL ("connection is not private")
───────────────────────────────────────────────────────────────────────

After Step 1, run this:

ssh root@ai.gladsw.cloud "cd /var/www/Ai-Career && sudo bash setup-ssl.sh"

This will:
✅ Install SSL certificate (Let's Encrypt)
✅ Fix "connection is not private" error
✅ Enable HTTPS for your site


═══════════════════════════════════════════════════════════════════════

📋 QUICK CHECK (What's wrong right now)
───────────────────────────────────────────────────────────────────────

From your logs:
❌ "upstream: http://127.0.0.1:5000"  ← WRONG! Should be 5001
❌ "Connection refused"                ← Because port 5000 has nothing
❌ "connection is not private"         ← SSL certificate missing

Backend is running fine on port 5001 ✅
Nginx is trying to connect to port 5000 ❌ (WRONG PORT!)


═══════════════════════════════════════════════════════════════════════

🎯 ONE COMMAND TO FIX EVERYTHING
───────────────────────────────────────────────────────────────────────

Copy-paste this ENTIRE line:

ssh root@ai.gladsw.cloud "cd /var/www/Ai-Career && git pull && bash emergency-fix-nginx.sh && sleep 3 && sudo bash setup-ssl.sh"


═══════════════════════════════════════════════════════════════════════

✅ After fix, test these:
───────────────────────────────────────────────────────────────────────

http://ai.gladsw.cloud          ← Should work (redirect to HTTPS)
https://ai.gladsw.cloud         ← Should work (no warning)
https://ai.gladsw.cloud/health  ← Should return JSON


═══════════════════════════════════════════════════════════════════════

🔍 Verify Fix Worked:
───────────────────────────────────────────────────────────────────────

ssh root@ai.gladsw.cloud "grep 'proxy_pass' /etc/nginx/sites-available/ai.gladsw.cloud"

Should show: proxy_pass http://localhost:5001;
NOT:         proxy_pass http://localhost:5000;


═══════════════════════════════════════════════════════════════════════

Bas yeh command run kar aur sab fix ho jayega! 🚀
